FROM python:3.12-slim

WORKDIR /app

# セキュリティ対策: 一般ユーザーを作成して実行
RUN useradd -m sandboxuser
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY server.py .

USER sandboxuser
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]